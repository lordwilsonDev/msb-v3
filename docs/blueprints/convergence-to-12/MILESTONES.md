# MSB v3 — Convergence Milestones (evidence gates, not dates)

**Opened:** 2026-08-16. A milestone is COMPLETE only when its exit evidence
exists (Blueprint Rule 2). Status: 🔴 not started · 🟡 in progress · 🟢 complete.

| M | Name | Status | Exit evidence (links) | Decision memo |
|---|---|---|---|---|
| M0 | Scope Lock and Baseline | 🟢 | v3 contract ✅ [`v3-contract.md`](v3-contract.md) · surface inventory ✅ [`surface-inventory.md`](surface-inventory.md) · baseline reproducible (portability gate + CI: lint/test/hygiene/security) · freeze active (Rule 3) | [2026-08-16: complete — contract + inventory committed; baseline evidenced by portability gate (1331 passed on last push) and CI gates; freeze active through M3.] |
| M1 | Core Loop Selection | 🟢 | Canonical workflow named + one rejected alternative · state machine · golden fixtures · run-id observability — [`M1-core-loop.md`](M1-core-loop.md) + [`fixtures/handle-loop/`](fixtures/handle-loop/) | [2026-08-16: complete. Chosen: governed agent handle loop (`/agent/handle`). Rejected: daily research digest (no consequential tool actions to govern). State machine mapped from `handle.py`; boundaries (input/intent/plan/gate/execute/observe/verify/record/report-recover) documented; golden fixtures committed incl. a live PASS run (`fb0b15ed6c48aedb`).] |
| M2 | Governance in the Loop | 🟢 | Guard on live path · fail-closed denials · complete evidence · observable governance metrics · bypass regression tests | [2026-08-16: complete. P0: `tests/governance/test_bypass.py` (13 tests) + `ACTIONGATE_DECISIONS` metric. P1: MCP chat surface verified governed (register_governed_tools → _run_governed); `_audit_append` now records explicit verdict (allowed/denied/approval-required/unknown/error); `tests/api/test_mcp_chat_governance.py` pins no-side-effect-on-deny + verdict records. P2: `tests/live/test_live_loop.py` (3 opt-in tests) walks MCP request through auth → gate → execute → evidence → audit-verify → replay. Total: 16 new tests.] |
| M3 | Shipping-Surface Convergence | 🟢 | No dateless shipping stubs · no misleading claims · before/after inventory · gates green | [2026-08-16: complete. S1 CUT (dead `/status` duplicate deleted, fields folded into live `studio.py` route + regression test); S3 CUT (`core/health.py` dead duplicate removed w/ its 3 tests); S4 CUT (`runtime_config.py`+`runtime.yaml` test-only aspirational config removed w/ 5 tests); S7 PARK (dated notes in both UAC module docstrings); H1 cleaned (both worktrees fully merged → removed + branches deleted); H3 PARK (Make.com workflow defs, external); H4 moved `verify_multi_tenant.py` → `scripts/`. Claims pass: README/MANIFEST/docs already honest (multimodal labeled stub-gated). Full battery green: lint 196 files, 1320 tests passed, hygiene 12/12, portability pending push gate.] |
| M4 | Factory Dogfood | 🟢 | Full factory run artifact · diverse reviewer real · seeded defect caught · no abandoned worktrees | [2026-08-16: complete — `tests/factory/test_factory_dogfood.py` (4 tests): real patch + worktree + pytest run MERGES with diverse panel (reviewer_models recorded, builder∉reviewers fail-closed); seeded BLOCK defect → BLOCKED with finding; CONCERN surfaced, never a silent merge. Worktrees: already clean (H1).] |
| M5 | Reliability & Adversarial Proof | 🟢 | Failure matrix implemented · no silent unsafe continuation · soak report · bounded recovery · security cases | [2026-08-17: complete — `tests/chaos/test_failure_matrix.py` (11 modes) + `tests/governance/test_bypass.py` (13 bypass invariants) all green, no P0/P1; live-proven denials (tainted-write 3× DENIED, kill-switch BLOCK) with replayable FAILED states; backup+restore drill over corrupted runtime → 19 DBs, chain valid; retrieval validated for phrase/token/empty/irrelevant queries. Soak folds into M6 trial — `failure-report-v0.3.0-rc1.md`.] |
| M6 | Personal Production Trial | 🟡 | ≥30 days usage · measurable value vs baseline · failure burden known · release driven by operating data | [2026-08-17: trial launched — workflow **Vault research-to-note** chosen + documented in `m6-trial.md`; one-command logger `scripts/trial-log.sh` (auto-captures run_id/verdict/hash/duration, auto-numbers entries); **15 ledger entries** (3 trial tasks logged today incl. the output-path finding → fixed via output_dir pass-through → verified). Weekly Friday review cadence defined; first review due 2026-08-22. Real 30-day trial in progress through 2026-09-16.]

[2026-08-17: **Week 1 review (automated rollup `scripts/trial-rollup.py` + 6 hermetic parser tests):** 15 tasks · 13 complete (86.7%) · 2 failed (002 retrieval-failure → fixed same week; 005 factory failed-closed → hardened, entries 007/008) · 7 interventions, ALL class=fix (zero approve/retry/bypass — the loop never needed operator unblocking) · median baseline 11 min vs MSB 38 s (~10 min saved per task) · evidence record useful on 15/15 (100%). **DECISION: KEEP the canonical workflow** (read-only + approved-write variants) — the two failures were caught, fixed, and regression-tested within the week; the read-only variant (Entry 015, 14 s) is the cheapest reliable shape and stays. Changes made this week (output_dir pass-through, retrieval fallback, intent-write suppression) remain. Supervision burden is code-fix, not babysitting — gate passes.] |
| M7 | Independent User Validation | 🟡 | ≥1 user completes task unaided · behavioral feedback · reproducible setup · understandable trust model | [2026-08-17: SCHEDULED + kit dry-run (user #0) DONE on a fresh clone: HTTPS clone → venv → lock install → .env → boot → canonical task PASS (run `dbb-20260817T225024-24682`, hash `8e87cef7d6d5c999`, 40s). Dry-run caught + fixed 2 real blockers: (1) launchd plist had a hardcoded builder machine path → agent un-startable elsewhere; now a path-neutral template rendered by start.sh before bootstrap; (2) run.sh ignored the venv the kit creates (hardcoded miniforge python) → now prefers `$REPO/.venv/bin/python`. Kit install docs updated with HTTPS + MSB_PORT notes. Recruit 2–3 candidates (sessions 09-17 → 10-01); 09-12 M6 gate = go/no-go; exit review 10-02 → M8.] |
| M8 | Public 12/10 Release | 🟡 | Auditable claims · reproducible demo · quantified results · prominent limitations · dated release decision | [2026-08-17: RC declared — `docs/releases/MSB-v3-RELEASE.md` + `v0.3.0-rc1-baseline.md` + `failure-report-v0.3.0-rc1.md`; canonical fixtures + run-report artifact committed. Remaining for M8: 30-day trial results (M6), independent-user report (M7), and the dated final release decision.] |

## Operating ledger (claim → evidence → gap → next action)

| Claim | Evidence | Gap | Next action |
|---|---|---|---|
| Governed tool loop works end-to-end | `/agent/handle` demo: plan → execute → verify → evidence; 1288→1331 tests green | Only exercised via API/CLI, not as the M1 canonical workflow | M1: pick the canonical workflow and run it through the full spine |
| Evidence + replay reconstruct a run | Phase 2/3 tests: decision→execution→verification vertebrae, replay consistency | No independent-engineer reconstruction demonstration | M4/M8: publish a full reconstruction from evidence alone |
| Audit chain is tamper-evident | Append-only triggers, hardened repair(), anchor + off-box notary + RFC 3161, signing seam | Anchor key still on-box (documented trust boundary) | Operator: provision Secure Enclave/YubiKey |
| Factory does independent review | `ReviewPanel` builder∉reviewers invariant, fail-closed LLM experts | No real MSB change has gone through the factory yet | M4: dogfood one real change |
| Multimodal is isolated | Flag-gated 503 + both-side tests | Stub code remains in shipping tree, but fail-closed and honestly labeled | M3: decision made — PARK (flag-gated, dated decision, outside default release path). Revisit post-M7. |

## Parking lot (Rule 3 — dated, with trigger)

Entries live in [`v4-parking-lot.md`](v4-parking-lot.md) (dated 2026-08-17):
strong sandbox · full tenant isolation · multimodal · distributed mesh ·
agent factory · autonomous evolution · DB schema versioning · deleted-file
diffs · sensor/actuator/experiment plane. Also rejected-for-v3: more agent
personas, polished dashboards, generalized autonomy. Leave the parking lot
only via a dated exception naming the evidence that justifies it (freeze rule).
